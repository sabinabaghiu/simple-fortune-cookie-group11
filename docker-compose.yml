services:
  backend:
    image: sabinabaghiu/backend:latest
    ports:
      - "9000:9000"

  frontend:
    image: sabinabaghiu/frontend:latest
    ports:
      - "8080:8080"
    depends_on:
      - backend